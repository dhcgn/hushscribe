# APM dependencies
apm_modules/

# Local development credentials — NEVER commit these.
.env
.env.*
!.env.example

node_modules/
dist/
playwright-report/
test-results/

# ~84 MB of real speech for the opt-in smoke suite (npm run test:smoke).
# Deliberately kept out of git: CI never uses it, and the GUI suite generates
# its own fixtures. Delete this line if you want it versioned.
test-data/
