# APM dependencies
apm_modules/

# Local development credentials — NEVER commit these.
.env
.env.*
!.env.example

node_modules/
dist/
playwright-report/
test-results/

# Vitest coverage output
coverage/

# Claude Code skills: junction into .agents/skills (created locally, not portable)
.claude/skills/

# devcontainer CLI log: devcontainer up ... | tee devcontainer-up.log
devcontainer-up.log
